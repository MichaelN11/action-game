#pragma once

enum class Keybind { escape, left, right, up, down };

enum class DrawState { standLeft, standRight, standUp, standDown, walkLeft, walkRight, walkUp, walkDown };

enum class Group { none, player, enemy };